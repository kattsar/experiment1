output_files : each folder corresponds to one participant; each folder contains the PsychoPy output excel file and the Praat output file for that participant (the latter in both .txt and .csv formats); the files are not merged.

data_processed : 
	- Audio_files : each folder corresponds to one participant and contains the audio files as well as the textgrid for each audio file; it also 	contains the Praat output file for each participant (TestDataKaterina)
	- PsychoPy_output : contains the excel output files from PsychoPy for each participant
	- test: contains pre-piloting audio and PsychoPy output files 


data_raw : contains the caja output files for each participant (training and caja test); also contains the audio and PSychoPy output excel files from Svetlana's testing of the experiment in Korta